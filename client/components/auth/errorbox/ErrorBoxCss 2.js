import React, { useState } from "react";
import { Alert, Modal, StyleSheet, Text, Pressable, View } from "react-native"

const errorBoxCss = StyleSheet.create({
textCss: {
  left: 20, 
  
}, 
shadowBox: {
  right: 20,
  borderRadius: 10,
  borderColor: "#f0ffff",
width: 205,
backgroundColor: '#f0f8ff',
shadowColor: "#1e90ff", 
shadowOpacity: 1,
shadowRadius: 5,
padding: 10,
shadowOffset: {width: 1,height: 1},
},
})

export default errorBoxCss